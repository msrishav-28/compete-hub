# Backend Environment Variables

# ======================
# SERVER CONFIGURATION
# ======================
PORT=8000
HOST=0.0.0.0
ENVIRONMENT=development  # development | staging | production

# ======================
# CORS CONFIGURATION
# ======================
# Comma-separated list of allowed origins
# In development:
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:5173

# In production (update with your actual frontend URL):
# CORS_ORIGINS=https://competehub.vercel.app

# ======================
# MONGODB ATLAS DATABASE
# ======================
# 1. Go to mongodb.com/atlas and create a FREE cluster
# 2. Create a database user (Security → Database Access)
# 3. Whitelist IPs (Security → Network Access → Add 0.0.0.0/0 for Render)
# 4. Get connection string (Database → Connect → Connect your application)
# 5. Replace the placeholders below:

MONGODB_URL=mongodb+srv://YOUR_USERNAME:YOUR_PASSWORD@cluster0.xxxxx.mongodb.net/?retryWrites=true&w=majority

# Database name (optional, defaults to competehub)
DB_NAME=competehub

# ======================
# OPTIONAL CONFIGURATION
# ======================
# Cache TTL for competition data (hours)
CACHE_TTL_HOURS=24

# Kaggle API credentials (for enhanced Kaggle data fetching)
# KAGGLE_USERNAME=your_kaggle_username
# KAGGLE_KEY=your_kaggle_key
